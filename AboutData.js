/**
 *This object is provided to the OnAboutScript method, which is called when the user clicks the About button for a script in the Toolbars / Scripts Preferences page.
 */
class AboutData {
	/**
	 * <p>This is a handle to the parent window that the script should use if
	 * displaying a dialog via the {@link Dialog} object. Even though this is
	 * not a {@link Tab}, it can still be assigned to the
	 * <strong>Dialog.window</strong> property to set the parent window of the
	 * dialog.</p>
	 * @returns {int}
	 */

	window;
} //end class
