/**
 *The Viewers object is a collection of all currently open standalone image viewers. It can be obtained via the DOpus.viewers property
 */
class Viewers {
	/**
	 * <p>Lets you enumerate the currently open viewers.</p>
	 * @returns {Viewer[]}
	 */

	_default_value
	/**
	 * <p>Returns a  {@link Viewer}&nbsp;object representing
	 *       the most recently active viewer window.</p>
	 * @returns {Viewer}
	 */

	lastactive;
} //end class
