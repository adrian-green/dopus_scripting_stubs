/**
 *This object is provided to the OnClick method, which is called whenever a script function is invoked (e.g. when the button is clicked or hotkey pressed).
 */
class ClickData {
	/**
	 * <p>Returns a <strong>Func</strong> object relating to this function. This
	 *       provides access to information about the function's environment - (source
	 *       and destination tabs, qualifier keys, etc).</p>
	 * @returns {Func}
	 */

	func;
} //end class
