/**
 *This object is provided to the OnAddCommands method, which allows a script to add new internal commands.
 */
class AddCmdData {
	/**
	 * <p>Adds a new internal command to Opus. The returned  {@link ScriptCommand}&nbsp;object
	 *       must be properly initialized. A script add-in can add as many internal
	 *       commands as it likes to the Opus internal command
	 * set.</p>
	 * @returns {ScriptCommand}
	 */
	AddCommand(){}
} //end class
